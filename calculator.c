#include <stdio.h>
#include <conio.h>
int main()
{
    int a, b, mod, sum;
    printf("Enter any two numbers a and b");
    scanf("%d,%d", &a, &b);
    mod = a % b;
    printf("the remainder is:%d", &mod);
    int sum()
    {
        int add;
        add = a + b;
        return add;
    }
    printf("The sum is %d", sum);
    return 0;
}