#include <stdio.h>
#include <conio.h>

int main()
{
    int a, b, sum;
    printf("Enter the two number.\n");
    scanf("%d%d", &a, &b);
    sum = a + b;
    printf("The sum is %d.", sum);
    return 0;
}